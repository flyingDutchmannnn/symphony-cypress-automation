

export default class PostCoreApiDataModel {

    //properties
    postId: string;
    createdOn: string;
    text: string

    constructor() {
        this.postId = this.postId
        this.createdOn = this.createdOn
        this.text = this.text
    }
}